<div class="container"></div>
    <div class="row ">   
        <div class="col s12 m6 offset-m3">
            <div class="card blue-grey darken-3">
                <div class="card-content white-text"></div>
                    {{#each posts as |post|}}

                    <p class="center-align card-content white-text">{{ this.name }}</p>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</div>